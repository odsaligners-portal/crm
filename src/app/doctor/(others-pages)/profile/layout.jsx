
export const metadata = {
  title: "Profile | ODS CRM",
  description:
    "Profile | ODS CRM",
};

const ProfileLayout = ({ children }) => {
  return <>{children}</>;
};

export default ProfileLayout; 